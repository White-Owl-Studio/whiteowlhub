---
import Base from '../layouts/Base.astro';

const experiences = [
  {
    id: 'lailas-descent',
    title: "Laila's Descent",
    tagline: "She came looking for the exit. There isn't one.",
    eyebrow: 'Horror Shooter',
    status: 'demo',
    url: '/experiences/lailas-descent',
    weight: 0.50,
    swirlR: 238, swirlG: 34,  swirlB: 34,
    hasCharacter: true,
  },
  {
    id: 'placeholder-a',
    title: '[Untitled Experience]',
    tagline: 'Something is being assembled beneath this surface.',
    eyebrow: 'Coming Soon',
    status: 'conjuring',
    url: null,
    weight: 0.24,
    swirlR: 120, swirlG: 224, swirlB: 88,
    hasCharacter: false,
  },
  {
    id: 'placeholder-b',
    title: '[No Name Yet]',
    tagline: 'Not ready. Possibly not safe.',
    eyebrow: 'Coming Soon',
    status: 'conjuring',
    url: null,
    weight: 0.24,
    swirlR: 112, swirlG: 64,  swirlB: 184,
    hasCharacter: false,
  },
  {
    id: 'secret',
    title: '⊹  ⊹  ⊹',
    tagline: "You found something that doesn't have a name yet. Check back.",
    eyebrow: '',
    status: 'unknown',
    url: null,
    weight: 0.02,
    swirlR: 245, swirlG: 208, swirlB: 96,
    hasCharacter: false,
  },
];
---

<Base title="Online Playthings" description="Interactive experiences from White Owl Studio.">

  <section class="xp-hero">

    <p class="eyebrow">Online Playthings</p>

    <div class="orb-stage">

      <button
        class="orb-wrap"
        id="orb-wrap"
        type="button"
        aria-label="Press to reveal an experience from White Owl Studio"
      >
        <canvas id="orb-canvas" aria-hidden="true"></canvas>
        <div class="orb-rim" id="orb-rim" aria-hidden="true"></div>
      </button>

      <div class="orb-reveal" id="orb-reveal" aria-hidden="true">
        <p class="reveal-eyebrow" id="reveal-eyebrow"></p>
        <h2 class="reveal-title"  id="reveal-title"></h2>
        <p class="reveal-tagline" id="reveal-tagline"></p>
        <div class="reveal-actions">
          <a  class="btn-enter" id="btn-enter" href="#">Enter</a>
          <button class="btn-again" id="btn-again" type="button">Press again ↺</button>
        </div>
      </div>

    </div>

  </section>

</Base>

<style>
  .xp-hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: var(--section-py) var(--section-px);
    gap: clamp(1.75rem, 4vw, 3rem);
  }

  .eyebrow {
    font-family: 'Cinzel', serif;
    font-size: 0.65rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--accent-experiences);
    opacity: 0.8;
    text-align: center;
  }

  /* ════════════ ORB ════════════ */
  .orb-stage {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: clamp(1.5rem, 3vw, 2.25rem);
  }

  .orb-wrap {
    --orb-size: clamp(280px, 50vmin, 520px);
    position: relative;
    width: var(--orb-size);
    height: var(--orb-size);
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    border-radius: 50%;
    outline: none;
    flex-shrink: 0;
  }
  .orb-wrap:focus-visible { box-shadow: 0 0 0 3px rgba(160,80,220,0.5); }

  #orb-canvas {
    display: block;
    width: 100%;
    height: 100%;
    border-radius: 50%;
  }

  /* Aurora rim — no hard outline, soft planetary glow */
  .orb-rim {
    position: absolute;
    inset: 0;
    border-radius: 50%;
    pointer-events: none;
    /* No border, only diffuse glow */
    box-shadow:
      0 0 48px 18px rgba(110,40,200,0.22),
      0 0 90px  32px rgba(80,20,160,0.14),
      0 0 150px 50px rgba(50,10,110,0.07);
    animation: aurora-breathe 5s ease-in-out infinite;
    transition: box-shadow 0.7s ease;
  }
  @keyframes aurora-breathe {
    0%, 100% { opacity: 0.75; }
    50%       { opacity: 1; }
  }
  .orb-wrap:hover .orb-rim {
    box-shadow:
      0 0 60px 22px rgba(130,50,220,0.30),
      0 0 110px 40px rgba(90,25,180,0.18),
      0 0 180px 60px rgba(60,10,130,0.09);
  }

  /* ════════════ REVEAL PANEL ════════════ */
  .orb-reveal {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.45rem;
    text-align: center;
    min-height: 130px;
    justify-content: center;
    max-width: 480px;
    transition: opacity 0.4s ease;
  }
  .orb-reveal[aria-hidden='true']  { opacity: 0; pointer-events: none; }
  .orb-reveal[aria-hidden='false'] { opacity: 1; }

  .reveal-eyebrow {
    font-family: 'Cinzel', serif;
    font-size: 0.6rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--reveal-color, var(--accent-experiences));
    opacity: 0.75;
    min-height: 1em;
  }
  .reveal-title {
    font-family: 'Cinzel', serif;
    font-size: clamp(1.4rem, 4vw, 2.4rem);
    font-weight: 400;
    color: var(--text);
    letter-spacing: 0.04em;
    line-height: 1.15;
    text-shadow: 0 0 32px var(--reveal-color, transparent);
  }
  .reveal-tagline {
    font-family: 'Crimson Pro', serif;
    font-style: italic;
    font-size: clamp(0.9rem, 1.8vw, 1.1rem);
    color: var(--text-muted);
    max-width: 360px;
    line-height: 1.55;
  }
  .reveal-actions {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    margin-top: 0.6rem;
    flex-wrap: wrap;
    justify-content: center;
  }
  .btn-enter {
    font-family: 'Cinzel', serif;
    font-size: 0.62rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--reveal-color, var(--accent-experiences));
    border: 1px solid var(--reveal-color, var(--accent-experiences));
    padding: 0.6rem 1.75rem;
    text-decoration: none;
    transition: background 0.22s, color 0.22s;
    display: inline-block;
  }
  .btn-enter:hover {
    background: var(--reveal-color, var(--accent-experiences));
    color: var(--bg);
  }
  .btn-enter[aria-hidden='true'] { display: none; }
  .btn-again {
    font-family: 'Cinzel', serif;
    font-size: 0.56rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--text-muted);
    background: none;
    border: none;
    cursor: pointer;
    padding: 0.4rem;
    opacity: 0.55;
    transition: opacity 0.2s, color 0.2s;
  }
  .btn-again:hover { opacity: 1; color: var(--text); }

  @media (max-width: 480px) {
    .xp-hero { justify-content: flex-start; padding-top: clamp(80px, 15vw, 120px); }
  }
</style>

<script define:vars={{ experiences }}>
(function () {
  /* ─── DOM ──────────────────────────────────────────────────────────── */
  const wrap     = document.getElementById('orb-wrap');
  const canvas   = document.getElementById('orb-canvas');
  const rim      = document.getElementById('orb-rim');
  const revPanel = document.getElementById('orb-reveal');
  const revEye   = document.getElementById('reveal-eyebrow');
  const revTitle = document.getElementById('reveal-title');
  const revTag   = document.getElementById('reveal-tagline');
  const btnEnter = document.getElementById('btn-enter');
  const btnAgain = document.getElementById('btn-again');
  if (!canvas || !wrap) return;

  /* ─── Canvas init ───────────────────────────────────────────────────── */
  const dpr = Math.min(window.devicePixelRatio || 1, 2);
  const CSS = wrap.clientWidth;
  canvas.width  = CSS * dpr;
  canvas.height = CSS * dpr;
  const ctx = canvas.getContext('2d');
  ctx.scale(dpr, dpr);
  const W = CSS, H = CSS, CX = W / 2, CY = H / 2, R = W * 0.48;

  /* ─── State ─────────────────────────────────────────────────────────── */
  let state       = 'idle';
  let currentExp  = null;
  let pendingExp  = null;
  let revAlpha    = 0;
  let bgAlpha     = 0;    // 0 = neutral purple, 1 = experience bg
  let swirlParts  = [];
  let swirlStart  = 0;
  let swirlDir    = 'inward';
  let swirlDur    = 960;
  let swirlCol    = [100, 40, 200];
  let secretTimer = null;

  /* ─── Neutral background: dark purple fog wisps + glitter ──────────── */
  const fogWisps = Array.from({ length: 5 }, (_, i) => ({
    ang:   (i / 5) * Math.PI * 2,
    rad:   R * (0.18 + i * 0.10),
    dAng:  (0.00022 + i * 0.00007) * (i % 2 ? 1 : -1),
    sz:    R * (0.55 + i * 0.10),
    op:    0.045 + i * 0.012,
  }));

  const neutralGlitter = Array.from({ length: 48 }, () => ({
    ang:  Math.random() * Math.PI * 2,
    rad:  R * (0.08 + Math.random() * 0.72),
    dAng: (0.0005 + Math.random() * 0.0009) * (Math.random() > 0.5 ? 1 : -1),
    sz:   0.5 + Math.random() * 1.8,
    op:   0.12 + Math.random() * 0.42,
    hue:  255 + Math.floor(Math.random() * 60),  // purple–violet band
  }));

  function drawNeutralBg(alpha) {
    // Always advance positions
    fogWisps.forEach(w => { w.ang += w.dAng; });
    neutralGlitter.forEach(p => { p.ang += p.dAng; });
    if (alpha <= 0.01) return;

    ctx.save();
    ctx.globalAlpha = alpha;

    // Dark purple void base
    const g = ctx.createRadialGradient(CX, CY, 0, CX, CY, R);
    g.addColorStop(0,   '#1c0a38');
    g.addColorStop(0.5, '#0e051e');
    g.addColorStop(1,   '#07030f');
    ctx.fillStyle = g;
    ctx.fillRect(0, 0, W, H);

    // Rotating fog wisps
    fogWisps.forEach(w => {
      const wx = CX + Math.cos(w.ang) * w.rad;
      const wy = CY + Math.sin(w.ang) * w.rad;
      const wg = ctx.createRadialGradient(wx, wy, 0, wx, wy, w.sz);
      wg.addColorStop(0, `rgba(130,50,220,${w.op})`);
      wg.addColorStop(1, 'rgba(80,15,160,0)');
      ctx.fillStyle = wg;
      ctx.beginPath();
      ctx.arc(wx, wy, w.sz, 0, Math.PI * 2);
      ctx.fill();
    });

    // Orbiting glitter
    neutralGlitter.forEach(p => {
      const px = CX + Math.cos(p.ang) * p.rad;
      const py = CY + Math.sin(p.ang) * p.rad;
      ctx.beginPath();
      ctx.arc(px, py, p.sz, 0, Math.PI * 2);
      ctx.fillStyle = `hsla(${p.hue},65%,72%,${p.op * alpha})`;
      ctx.fill();
    });

    ctx.restore();
  }

  function drawExpBg(exp, alpha, ts) {
    if (alpha <= 0.01) return;
    ctx.save();
    ctx.globalAlpha = alpha;

    const r = exp.swirlR, g = exp.swirlG, b = exp.swirlB;
    const bg = ctx.createRadialGradient(CX, CY - R*0.1, 0, CX, CY, R);
    bg.addColorStop(0,   `rgba(${Math.round(r*0.14)},${Math.round(g*0.14)},${Math.round(b*0.14)},1)`);
    bg.addColorStop(0.6, `rgba(${Math.round(r*0.05)},${Math.round(g*0.05)},${Math.round(b*0.05)},1)`);
    bg.addColorStop(1,   'rgba(4,3,8,1)');
    ctx.fillStyle = bg;
    ctx.fillRect(0, 0, W, H);

    // Laila's Descent: add Zone 1 cold light from above
    if (exp.id === 'lailas-descent') {
      const pulse = 0.5 + 0.5 * Math.sin(ts * 0.00042);
      const shimmer = ctx.createRadialGradient(CX, CY - R*0.7, 0, CX, CY - R*0.45, R*0.55);
      shimmer.addColorStop(0, `rgba(184,212,240,${0.09 * pulse})`);
      shimmer.addColorStop(1, 'rgba(184,212,240,0)');
      ctx.fillStyle = shimmer;
      ctx.fillRect(0, 0, W, H);
    }

    ctx.restore();
  }

  /* ─── Swirl particles ───────────────────────────────────────────────── */
  function spawnSwirl(dir, col, n = 92) {
    swirlParts = [];
    swirlCol   = col;
    for (let i = 0; i < n; i++) {
      const ang    = Math.random() * Math.PI * 2;
      const startR = dir === 'inward' ? R*(0.80+Math.random()*0.18) : R*(0.01+Math.random()*0.06);
      const endR   = dir === 'inward' ? R*(0.01+Math.random()*0.05) : R*(0.78+Math.random()*0.20);
      swirlParts.push({
        ang, startR, endR, curR: startR,
        dAng: (dir === 'inward' ? 1 : -1) * (0.018 + Math.random() * 0.04),
        sz:   0.7 + Math.random() * 2.4,
        op:   0.35 + Math.random() * 0.60,
      });
    }
  }

  function drawSwirl(elapsed) {
    if (!swirlParts.length) return;
    const prog = Math.min(elapsed / swirlDur, 1);
    const ease = swirlDir === 'inward'
      ? 1 - Math.pow(1 - prog, 2.8)
      : Math.pow(prog, 1.8);
    const [sr, sg, sb] = swirlCol;

    swirlParts.forEach(p => {
      p.ang  += p.dAng;
      p.curR  = p.startR + (p.endR - p.startR) * ease;
      const fadeOp = p.op * (1 - Math.pow(prog, 1.6));
      if (fadeOp < 0.02) return;
      const px = CX + Math.cos(p.ang) * p.curR;
      const py = CY + Math.sin(p.ang) * p.curR;
      // Halo
      ctx.save();
      ctx.globalAlpha = fadeOp * 0.3;
      ctx.beginPath();
      ctx.arc(px, py, p.sz * 3.2, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(${sr},${sg},${sb},0.5)`;
      ctx.fill();
      ctx.restore();
      // Core spark
      ctx.beginPath();
      ctx.arc(px, py, p.sz, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(${sr},${sg},${sb},${fadeOp})`;
      ctx.fill();
    });
  }

  /* ─── Catmull-Rom helper — smooth curve that passes THROUGH each point ─ */
  function crPath(pts) {
    ctx.moveTo(pts[0][0], pts[0][1]);
    for (let i = 0; i < pts.length - 1; i++) {
      const p0 = pts[Math.max(0, i - 1)];
      const p1 = pts[i];
      const p2 = pts[i + 1];
      const p3 = pts[Math.min(pts.length - 1, i + 2)];
      ctx.bezierCurveTo(
        p1[0] + (p2[0] - p0[0]) / 6,  p1[1] + (p2[1] - p0[1]) / 6,
        p2[0] - (p3[0] - p1[0]) / 6,  p2[1] - (p3[1] - p1[1]) / 6,
        p2[0], p2[1]
      );
    }
  }

  /* ─── Laila sprite ──────────────────────────────────────────────────── */
  function drawLaila(alpha, ts) {
    if (alpha <= 0.01) return;
    const float = Math.sin(ts * 0.00085) * (R * 0.022);
    const ox = CX, oy = CY - R * 0.06 + float;
    const s  = (R * 0.58) / 83;   // game units → CSS px

    ctx.save();
    ctx.globalAlpha = alpha;

    // Red glow
    const glow = ctx.createRadialGradient(ox, oy, 0, ox, oy, R * 0.48);
    glow.addColorStop(0, 'rgba(238,34,34,0.10)');
    glow.addColorStop(1, 'rgba(238,34,34,0)');
    ctx.fillStyle = glow;
    ctx.beginPath();
    ctx.arc(ox, oy, R * 0.48, 0, Math.PI * 2);
    ctx.fill();

    // ── Dress ──
    ctx.beginPath();
    ctx.moveTo(ox - 11*s, oy - 6*s);
    ctx.bezierCurveTo(ox - 20*s, oy + 18*s, ox - 16*s, oy + 44*s, ox - 7*s, oy + 58*s);
    ctx.lineTo(ox + 7*s, oy + 58*s);
    ctx.bezierCurveTo(ox + 16*s, oy + 44*s, ox + 20*s, oy + 18*s, ox + 11*s, oy - 6*s);
    ctx.closePath();
    ctx.fillStyle = 'rgba(252,250,243,0.90)';
    ctx.fill();
    ctx.beginPath();
    ctx.moveTo(ox - 2*s, oy - 4*s);
    ctx.bezierCurveTo(ox - 2*s, oy + 20*s, ox - 1*s, oy + 42*s, ox - 1*s, oy + 58*s);
    ctx.strokeStyle = 'rgba(200,195,178,0.22)';
    ctx.lineWidth = 1.5 * s;
    ctx.stroke();

    // ── Arms ──
    ctx.lineWidth = 5.5 * s;
    ctx.lineCap   = 'round';
    ctx.strokeStyle = 'rgba(248,228,206,0.86)';
    ctx.beginPath();
    ctx.moveTo(ox - 11*s, oy + 4*s);
    ctx.bezierCurveTo(ox - 26*s, oy + 1*s, ox - 38*s, oy - 7*s, ox - 43*s, oy - 4*s);
    ctx.stroke();
    ctx.beginPath();
    ctx.moveTo(ox + 11*s, oy + 4*s);
    ctx.bezierCurveTo(ox + 26*s, oy + 1*s, ox + 38*s, oy - 7*s, ox + 43*s, oy - 4*s);
    ctx.stroke();

    // ── Hair L1 (back layer) — drawn BEFORE head so face covers it ──
    // Catmull-rom through approved waypoints: curve actually passes through each point
    // neckR(4,-4.3) earR(17.8,-8.5) wingR(25.5,-19.1) tipR(17.8,-18.1)
    // topR(6,-24.9) crown(0,-23.6) neckBot(0,+4)
    ctx.beginPath();
    crPath([
      [ox + 4*s,    oy - 4.3*s],   // neckR
      [ox + 17.8*s, oy - 8.5*s],   // earR
      [ox + 25.5*s, oy - 19.1*s],  // wingR — signature flare
      [ox + 17.8*s, oy - 18.1*s],  // tipR
      [ox + 6*s,    oy - 24.9*s],  // topR
      [ox,          oy - 23.6*s],  // crown
      [ox,          oy + 4*s],     // neckBot
    ]);
    ctx.closePath();
    ctx.fillStyle = '#ee2222';
    ctx.fill();

    // ── Head — drawn AFTER L1 hair so face sits in front ──
    ctx.beginPath();
    ctx.arc(ox, oy - 18*s, 13*s, 0, Math.PI * 2);
    ctx.fillStyle = 'rgba(248,228,208,0.95)';
    ctx.fill();

    // ── Hair L2 (front bangs) — on top of face ──
    // rOuter(11.8,-10.6) rTop(5,-22) rInner(0,-17.8) rBot(5,-13)
    ctx.beginPath();
    crPath([
      [ox + 5*s,    oy - 13*s],    // rBot
      [ox + 11.8*s, oy - 10.6*s],  // rOuter
      [ox + 5*s,    oy - 22*s],    // rTop
      [ox,          oy - 17.8*s],  // rInner
    ]);
    ctx.closePath();
    ctx.fillStyle = '#ee2222';
    ctx.fill();

    // ── Eyes ──
    const ey = oy - 19*s, er = 4.5*s;
    ctx.beginPath(); ctx.arc(ox - 5*s, ey, er, 0, Math.PI*2); ctx.fillStyle = '#33bb44'; ctx.fill();
    ctx.beginPath(); ctx.arc(ox + 5*s, ey, er, 0, Math.PI*2); ctx.fill();
    ctx.fillStyle = 'rgba(8,4,16,0.92)';
    ctx.beginPath(); ctx.arc(ox - 5*s, ey, 2.7*s, 0, Math.PI*2); ctx.fill();
    ctx.beginPath(); ctx.arc(ox + 5*s, ey, 2.7*s, 0, Math.PI*2); ctx.fill();
    ctx.fillStyle = 'rgba(255,255,255,0.88)';
    ctx.beginPath(); ctx.arc(ox - 3.8*s, ey - 1.5*s, 1.2*s, 0, Math.PI*2); ctx.fill();
    ctx.beginPath(); ctx.arc(ox + 6.2*s, ey - 1.5*s, 1.2*s, 0, Math.PI*2); ctx.fill();

    ctx.restore();
  }

  /* ─── DOM show/hide ────────────────────────────────────────────────── */
  function showReveal(exp) {
    const rc = `rgb(${exp.swirlR},${exp.swirlG},${exp.swirlB})`;
    revPanel.style.setProperty('--reveal-color', rc);
    revEye.textContent   = exp.eyebrow;
    revTitle.textContent = exp.title;
    revTag.textContent   = exp.tagline;

    if (exp.url) {
      btnEnter.href = exp.url;
      btnEnter.removeAttribute('aria-hidden');
    } else {
      btnEnter.setAttribute('aria-hidden', 'true');
    }

    // Aurora rim shifts to experience colour — still diffuse, no hard outline
    rim.style.boxShadow = `
      0 0 55px 20px rgba(${exp.swirlR},${exp.swirlG},${exp.swirlB},0.24),
      0 0 100px 36px rgba(${exp.swirlR},${exp.swirlG},${exp.swirlB},0.13),
      0 0 160px 55px rgba(${exp.swirlR},${exp.swirlG},${exp.swirlB},0.06)
    `;
    revPanel.setAttribute('aria-hidden', 'false');

    if (exp.id === 'secret') {
      secretTimer = setTimeout(() => {
        if (state === 'revealed' && currentExp?.id === 'secret') resetToIdle();
      }, 7000);
    }
  }

  function resetToIdle() {
    clearTimeout(secretTimer);
    state      = 'idle';
    revAlpha   = 0;
    bgAlpha    = 0;
    currentExp = null;
    swirlParts = [];
    rim.style.boxShadow = '';   // revert to CSS animation
    revPanel.setAttribute('aria-hidden', 'true');
  }

  /* ─── Press handler ────────────────────────────────────────────────── */
  function handlePress() {
    if (state === 'swirling') return;

    if (state === 'idle') {
      currentExp = pickExp(null);
      spawnSwirl('inward', [currentExp.swirlR, currentExp.swirlG, currentExp.swirlB]);
      swirlDir = 'inward'; swirlDur = 960;
      swirlStart = performance.now();
      revAlpha = 0;
      state = 'swirling';

    } else if (state === 'revealed') {
      pendingExp = pickExp(currentExp);
      spawnSwirl('outward', [currentExp.swirlR, currentExp.swirlG, currentExp.swirlB], 70);
      swirlDir = 'outward'; swirlDur = 500;
      swirlStart = performance.now();
      state = 'swirling';
      revPanel.setAttribute('aria-hidden', 'true');
    }
  }

  function pickExp(exclude) {
    const pool  = experiences.filter(e => !exclude || e.id !== exclude.id);
    const total = pool.reduce((s, e) => s + e.weight, 0);
    let   roll  = Math.random() * total;
    for (const e of pool) { roll -= e.weight; if (roll <= 0) return e; }
    return pool[pool.length - 1];
  }

  /* ─── Render loop ───────────────────────────────────────────────────── */
  let rafId;
  function render(ts) {
    ctx.clearRect(0, 0, W, H);

    ctx.save();
    ctx.beginPath();
    ctx.arc(CX, CY, R, 0, Math.PI * 2);
    ctx.clip();

    // ── Background crossfade ──
    if (state === 'swirling') {
      const elapsed = ts - swirlStart;
      bgAlpha = swirlDir === 'inward'
        ? Math.min(elapsed / swirlDur, 1)
        : Math.max(1 - elapsed / swirlDur, 0);
    } else if (state === 'revealed') {
      bgAlpha = 1;
    } else {
      bgAlpha = 0;
    }

    drawNeutralBg(1 - bgAlpha);
    if (bgAlpha > 0.01 && currentExp) drawExpBg(currentExp, bgAlpha, ts);

    // ── Swirl & character ──
    if (state === 'swirling') {
      const elapsed = ts - swirlStart;
      const prog    = elapsed / swirlDur;

      drawSwirl(elapsed);

      if (swirlDir === 'inward') {
        if (elapsed > swirlDur * 0.62 && currentExp?.hasCharacter) {
          revAlpha = Math.min((elapsed - swirlDur*0.62) / (swirlDur*0.45), 1);
          drawLaila(revAlpha, ts);
        }
        if (prog >= 1) {
          state = 'revealed'; revAlpha = 1; swirlParts = [];
          showReveal(currentExp);
        }
      } else {
        revAlpha = Math.max(1 - elapsed / (swirlDur * 0.65), 0);
        if (currentExp?.hasCharacter) drawLaila(revAlpha, ts);
        if (prog >= 1) {
          currentExp = pendingExp;
          spawnSwirl('inward', [currentExp.swirlR, currentExp.swirlG, currentExp.swirlB]);
          swirlDir = 'inward'; swirlDur = 960; swirlStart = ts; revAlpha = 0;
        }
      }
    }

    if (state === 'revealed') {
      if (currentExp?.hasCharacter) drawLaila(revAlpha, ts);
      // Ambient glitter shimmer in revealed state
      if (Math.random() < 0.018) {
        const [sr, sg, sb] = [currentExp.swirlR, currentExp.swirlG, currentExp.swirlB];
        for (let i = 0; i < 4; i++) {
          const a = Math.random() * Math.PI * 2;
          const r = R * (0.25 + Math.random() * 0.58);
          ctx.beginPath();
          ctx.arc(CX + Math.cos(a)*r, CY + Math.sin(a)*r, 0.8 + Math.random()*1.2, 0, Math.PI*2);
          ctx.fillStyle = `rgba(${sr},${sg},${sb},${0.28 + Math.random()*0.45})`;
          ctx.fill();
        }
      }
    }

    ctx.restore();
    rafId = requestAnimationFrame(render);
  }

  rafId = requestAnimationFrame(render);

  wrap.addEventListener('click', handlePress);
  wrap.addEventListener('keydown', e => {
    if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); handlePress(); }
  });
  btnAgain.addEventListener('click', e => { e.stopPropagation(); handlePress(); });
  document.addEventListener('astro:before-swap', () => cancelAnimationFrame(rafId));
})();
</script>
